- Make a command that pipes to the same output box as fzf does.
- Make a command that pipes to a tmp output file
  - global var for output file path

- global map to lookup command by filetype
- default map
- docs
